const textoNumero = "150.5";
const valorNulo = null;
const booleanoTexto = true;

console.log(`
    String => Number: ${Number(textoNumero)}
    Valor nulo: ${valorNulo}
    Boolean => String: ${booleanoTexto.toString()}
    `);